#include <behaviour_connection.h>

void execute_my_behaviour(struct behaviour_connection *conn) {
    // read from conn->pivot_angles
    // write to conn->wheel_commands
    // write to conn->flag
    conn->pivot_angles;
    conn->wheel_commands;
}
